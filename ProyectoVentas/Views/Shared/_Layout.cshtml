<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ProyectoVentas</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <style>
        /* Estilo personalizado para el menú hamburguesa */
        .hamburger-menu {
            position: relative;
        }

        .hamburger-menu .nav-menu {
            display: none;
            position: absolute;
            top: 40px;
            left: 0;
            background-color: #f8f9fa;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 10px;
            z-index: 1000;
        }

        .hamburger-menu input[type="checkbox"] {
            display: none;
        }

        .hamburger-menu input[type="checkbox"]:checked ~ .nav-menu {
            display: block;
        }

        .hamburger-icon {
            font-size: 24px;
            cursor: pointer;
            padding: 5px 10px;
        }
    </style>
</head>
<body>
    <!-- Menú hamburguesa -->
    <div class="hamburger-menu">
        <label class="hamburger-icon" for="menu-toggle">&#9776;</label>
        <input type="checkbox" id="menu-toggle" />
        <div class="nav-menu">
            <ul class="list-unstyled">
                <li><a href="/" class="text-dark">Inicio</a></li>
                <li><a href="/Productos" class="text-dark">Productos</a></li>
                <li><a href="/Ventas" class="text-dark">Ventas</a></li>
                <li><a href="/Contacto" class="text-dark">Contacto</a></li>
            </ul>
        </div>
    </div>

    <div class="container">
        @RenderBody()
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>